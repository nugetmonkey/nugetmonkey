apply plugin: 'java'
apply plugin: 'nugetmonkey'

dependencies {

}

println "${new FileNameByRegexFinder().getFileNames("$projectDir", /.*\...proj/).head()}"

NugetMonkey{
    refs "$rootDir/a.dll"
    projFile "$projectDir/TryJettyOnNet/TryJettyOnNet.csproj"
}